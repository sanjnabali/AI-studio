# Core FastAPI and web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# CORS and middleware
PyJWT>=2.0.0

# Password hashing
bcrypt==4.0.1
passlib[bcrypt]>=1.7.4

# AI and ML libraries (with fallback options)
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.2
accelerate>=0.24.0

# Optional AI libraries (comment out if causing issues)
# openai-whisper>=20231117
# diffusers>=0.24.0

# Audio processing (lightweight alternatives)
librosa>=0.10.1
soundfile>=0.12.1
pydub>=0.25.1

# Vector database and search
faiss-cpu>=1.7.4
numpy>=1.24.3

# Data processing
pandas>=2.1.0
pillow>=10.0.0

# Development and monitoring
python-dotenv>=1.0.0
logging-config>=1.0.3

# Optional performance boosters
bitsandbytes>=0.41.0  # For quantization
optimum>=1.14.0      # For model optimization
